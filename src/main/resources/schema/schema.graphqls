type Query {
    shows(titleFilter: String): [Show]
    dgsShows(releaseYear: Int): [Show]
    springShows(releaseYear: Int): [Show]
    springAllShows: [Show]
}

type Show {
    title: String
    releaseYear: Int
}
